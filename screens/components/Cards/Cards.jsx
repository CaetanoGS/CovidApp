import React from 'react';
import {StyleSheet, View, Text} from 'react-native'

const Cards = (props) => {
    console.log(props);
    return(
        <View>
            <Text>Cards</Text>
        </View>
    );
}

export default Cards;